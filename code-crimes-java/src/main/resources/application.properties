# Server Configuration
server.port=8080

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:producthub
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# H2 Console (for testing)
spring.h2-console.enabled=true
spring.h2-console.path=/h2-console

# CODE CRIME: Hardcoded values that should be externalized
app.tax.rate=0.08
app.shipping.standard=5.99
app.shipping.express=25.99
app.max.products=1000

# CODE CRIME: Commented out config without explanation
# spring.jpa.properties.hibernate.format_sql=true
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
